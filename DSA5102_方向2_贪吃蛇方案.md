# DSA5102 期末项目 · 方向 2 详细方案
## 用强化学习训练 AI 玩贪吃蛇（Snake）

> 课程：DSA5102 Foundations of Machine Learning（AY2025/26 S1）
> 截止：2026 年 11 月 22 日
> 选题：Option 2 — Reinforcement Learning
> 团队：5 人（视频 20 分钟，每人约 4 分钟）
> 算法路线：稳妥递进 —— DQN 基线 + 改进组件逐项对比

---

## 0. 一页总览

| 维度 | 决策 |
|---|---|
| 游戏 | Snake（贪吃蛇，默认 10×10 网格，有界棋盘） |
| MDP 建模 | 逐帧决策，动作 = 4 个离散方向（上/下/左/右，屏蔽 180° 掉头） |
| 状态 | 手工特征向量（11 维：危险信号 3 + 当前方向 4 + 食物相对方向 4）＋ 局部视野变体 |
| 奖励 | +10 吃食 / −10 死亡（基线）＋ 生存/效率/距离 shaping（实验变量） |
| 基线 | M0 随机 → M1 贪婪启发式（朝食物 + 安全一步）→ M2 vanilla DQN |
| 改进 | 逐项加入 DDQN → 优先经验回放(PER) → Dueling → n-step，消融对比 |
| 评估 | ≥3 随机种子学习曲线 + 固定 100 episode 终评（均值±std）+ 训练过程录像 |
| 框架 | Python + NumPy + **PyTorch** + gymnasium API（渲染用 Pygame 或 matplotlib） |
| 交付 | 可运行 Jupyter notebook（`TRAIN_MODEL=False` + 保存模型）+ 20 分钟真人视频 |

---

## 1. 选题论证

**为什么 Snake 是四款游戏里的「得分/风险」最优解：**

1. **训练快、CPU 可跑**。低维特征态 + 离散 4 动作 + 简单网格，单方法训练分钟级到几十分钟即可收敛，12 周里能从容跑完「多方法 × 多种子」的完整消融，**完成度与实验严谨性两条评分最稳**。
2. **进步可视化最有冲击力**。任务书硬性要求「可视化展示训练进步」，Snake 是最直观的：从「开局撞墙」到「蛇铺满半屏、长度 50+」的对比 montage，说服力碾压 Pong 的比分波动。
3. **改进空间最饱满**。奖励 shaping 与算法改进（DDQN/PER/Dueling/n-step）都能做出清晰的增量曲线；相比之下 Pong 所有方法过早饱和，**「对比不同方案」的区分度会被压平**。
4. **创意分好拿且不过度内卷**。Breakout/Pong 是全网做烂的 Atari 默认题；自写环境 + 特征工程 + 启发式基线对比，是一条有辨识度、且游戏复杂度适中（不 trivial 也不失控）的故事线。

**核心叙事**：报告写成「从启发式到学习式」的证据链——先建立随机/启发式双基线，再展示 DQN 及其改进如何逼近并超越启发式，每个改进给出可量化的增量。这比孤立调一个算法更符合「科学方法论」的考察点。

---

## 2. 核心技术设计：MDP 建模

### 2.1 游戏规则与环境

- 网格：默认 **10×10**（有界，撞墙即死），难度扩展实验用 15×15 / 20×20。
- 初始：蛇长 3，每 tick 前进一格；食物随机出现在空格（固定种子可复现）。
- 死亡：撞墙或撞自身身体。
- 掉头约束：蛇不能直接反向 180°（动作被屏蔽，或在环境中重采样）。

### 2.2 动作空间

4 个绝对方向：`UP / DOWN / LEFT / RIGHT`。每步把「反向动作」mask 掉（有效动作 2–3 个），ε-greedy 只在合法动作里采样。

### 2.3 状态表示（首选：11 维特征向量）

| 分量 | 维度 | 含义 |
|---|---|---|
| 危险信号 | 3 | 正前方 / 左 / 右 是否「撞墙或撞身」（二进制） |
| 当前方向 | 4 | 蛇头朝向 one-hot |
| 食物相对方向 | 4 | 食物在蛇头的 上/下/左/右（二进制，由坐标差符号给出） |

- 维度小、训练快，是 Snake 上被反复验证有效的经典表示。
- **实验变体（加分项）**：局部视野表示——以蛇头为中心的 7×7 二值窗口（墙/蛇身/食物通道）拼上方向 one-hot，喂给 CNN 或展平向量，对比「手工特征 vs 局部原始观测」。这条「表示学习」对比线本身就是一个漂亮的科学卖点。

### 2.4 奖励函数

基线（稀疏）：

- 吃到食物 **+10**；死亡 **−10**；其他步 **0**

Shaping 变体（作为「探索提升 AI 强度」的对比实验）：

| 变体 | 设计 | 目的 |
|---|---|---|
| V1 效率压力 | 基线 + 每空步 −0.01 | 减少绕圈、追求最短路径 |
| V2 生存压力 | 基线 + 每存活步 +0.1（配 episode 步数上限） | 鼓励存活，但可能诱发绕圈 |
| V3 距离 shaping | 朝食物移动 +小奖、远离 −小罚 | 引导进食，需防震荡 |

> 奖励 shaping 本身作为第 5 个「改进项」参与消融，回答「什么奖励能让蛇更强」——这是任务书「探索如何提升强度并对比」的直接落点。

---

## 3. 方法阶梯（满足「多方法对比」）

| 编号 | 方法 | 角色 | 说明 |
|---|---|---|---|
| M0 | Random | 下界基线 | 在合法动作里均匀随机 |
| M1 | 贪婪启发式 | **强参照基线** | 合法动作里选「离食物曼哈顿距离最近」；平局时做一步安全检查，避开会导致下一步必死的动作 |
| M2 | Vanilla DQN | 学习式基线 | 特征态 + MLP [11→128→128→4] + 经验回放 + 目标网络 |
| M3 | + Double DQN | 改进① | 分离动作选择与评估，抑制 Q 过估 |
| M4 | + Prioritized Replay | 改进② | 按 TD-error 优先级采样 |
| M5 | + Dueling | 改进③ | V(s) + A(s,a) 双流 |
| M6 | + n-step | 改进④ | 多步 bootstrap 加速信用传播 |

消融顺序固定：M2 → M3 → M4 → M5 → M6（逐项叠加），形成单一变量的证据链；奖励 shaping（2.4 的 V1–V3）作为正交的独立对比轴。每个方法 = 报告里一张对比表 + 一条学习曲线。

---

## 4. 实验设计与评估协议

1. **训练协议**：每方法跑 **≥3 个随机种子**，训练约 200k–500k 步（特征态 + MLP，CPU 上分钟级到几十分钟）；记录训练步 vs 平均 episode 得分/蛇长/存活步数。
2. **学习曲线**：均值曲线 + 阴影（±1 std），横轴训练步，纵轴 100-episode 滑动平均得分。满足任务书「learning curves」。
3. **终评**：每方法固定种子跑 **100 个 episode**，报告 mean ± std、中位数、最优、最差；箱线图对比。指标三件套：**总奖励、平均蛇长、存活步数**。
4. **训练进步可视化**：在 0% / 25% / 50% / 75% / 100% 训练进度各录一段实况，剪辑成对比 montage；另录最终 AI 完整演示。蛇变长、存活变久的效果一眼可见。
5. **统计严谨性**：多种子均值 ± std，必要时 bootstrap 置信区间或配对检验，体现「scientific methodology」。

---

## 5. 与任务书要求的逐条对齐

| 任务书要求 | 本方案落点 |
|---|---|
| 可视化展示训练进步 | 训练进度分段录像 montage |
| 探索提升强度 + 对比 | DDQN/PER/Dueling/n-step + 奖励 shaping 消融 |
| 含 baseline | Random + 贪婪启发式（双基线，含强基线） |
| 学习曲线 | 每方法 ≥3 种子均值±std |
| 多 episode 评估 | 固定 100 episode 终评 |
| 「假设我没玩过」介绍游戏 | 视频第 1 段讲清规则/吃食/撞墙/掉头约束 |
| 可运行 + 长训练附模型 | notebook `TRAIN_MODEL=False` + 保存 .pt 权重 |
| AI 使用声明 | 明确声明用途（代码辅助/文字润色等） |
| 真人出镜 | 5 段真人讲解，禁止合成音视频 |
| 贡献声明 | 分工见第 6 节，按贡献差异化评分 |

---

## 6. 团队分工（5 人，每人约 4 分钟展示）

| 成员 | 工作包 | 视频展示内容 |
|---|---|---|
| A | **游戏与环境**：Snake 规则、gymnasium 环境、碰撞/掉头约束、渲染 | 游戏介绍（假设没玩过）+ 环境架构 + 动作/奖励设计 |
| B | **状态与基线**：11 维特征/局部视野、Random、贪婪启发式 | 状态表示设计 + 启发式基线原理与表现 |
| C | **DQN 主算法**：Q 网络、回放、目标网络、训练循环、ε-greedy | DQN 原理 + 实现要点 + 基线训练结果 |
| D | **改进组件**：DDQN、PER、Dueling、n-step、奖励 shaping | 各改进的原理 + 消融实验对比 |
| E | **评估与整合**：学习曲线、100-episode 终评、录像剪辑、报告/视频统筹 | 评估协议 + 总体结论 + 局限与展望 |

---

## 7. 时间线（2026-09-01 → 2026-11-22）

| 周次（约） | 里程碑 |
|---|---|
| W1–2（9 月上） | 环境搭建 + 单元测试；Random/启发式基线跑通；游戏介绍素材 |
| W3–4（9 月中下） | DQN 基线跑通，第一条学习曲线 |
| W5–7（10 月上中） | 改进组件逐个实现 + 奖励 shaping 消融（D/E 同步搭评估脚本） |
| W8（10 月底） | 全方法多种子终训 + 100-episode 终评 |
| W9（11 月初） | 训练过程录像 + 图表素材定稿 |
| W10（11 月中） | Jupyter 报告撰写（essay 风格）+ 保存模型集成 |
| W11（11 月中下） | 视频录制 + 剪辑（20 分钟，真人出镜） |
| W12 前 2–3 天 | 内部预演 + 查合规清单 + 提前提交（留 buffer） |

---

## 8. 超参数起点（可调）

- 网格 10×10（默认）；难度扩展 15×15 / 20×20
- γ = 0.9（短视距游戏常用较低折扣）；ε: 1.0 → 0.01（约 100k 步退火）；每 4 步训练 1 次
- Replay buffer 100k；batch 128；目标网络每 1000 步更新
- Adam lr = 1e-3；loss 用 Huber（δ=1）
- PER: α = 0.6，β 从 0.4 退火到 1.0；n-step = 3
- 训练预算 ~200k–500k 步 / 方法 / 种子；episode 步数上限 1000（防绕圈死循环）

---

## 9. 风险与应对

| 风险 | 应对 |
|---|---|
| 蛇绕圈死循环（生存奖励诱发） | episode 步数上限 + 吃食奖励占主导 + 屏蔽反向动作 |
| 局部最优：只会追食物、不规划身体 | 特征态加危险信号；对比局部视野变体；如实写进报告 |
| 特征表示触及性能天花板 | 引入局部视野/CNN 表示作为对比实验，本身就是科学发现 |
| 训练不稳定（Q 过估、发散） | DDQN + Huber loss + 目标网络 + 多种子 |
| 环境有 bug | 单元测试（碰撞/掉头/吃食/死亡判定）+ 固定种子复现 |
| 视频超时扣分 | 每人精确 4 分钟脚本 + 彩排；总长压到 18–19 分钟留余量 |

---

## 10. 创意加分方向（可选，按时间取舍）

- **难度扩展**：网格 10→15→20，展示「任务越难，方法差距越明显」。
- **跨范式对比**：DQN 系 vs Policy Gradient（REINFORCE/PPO），覆盖 value-based 与 policy-based。
- **表示对比**：手工特征 vs 局部原始观测（CNN），回答「特征工程还剩多少价值」。
- **双蛇/障碍物/移动食物**：提升复杂度与观赏性。

---

## 11. 合规清单（提交前逐项核对）

- [ ] notebook 原样可运行，长训练处 `TRAIN_MODEL=False` 并附 .pt 模型
- [ ] 生成式 AI 使用已声明（用途 + 方式），未声明即视为抄袭（零分条款）
- [ ] 贡献声明明确（全员均等 或 按人分工）
- [ ] 视频 20 分钟、每人约 4 分钟、**真人出镜**、无合成音视频（零分条款）
- [ ] 游戏介绍含可视化（「假设没玩过」口径）
- [ ] 环境代码/权重一并提交或附链接

---

## 12. 交付物清单

1. 可运行 Jupyter notebook（项目报告，essay 风格，含 markdown 说明）
2. 20 分钟真人视频（5 × 4 分钟）
3. 自建 Snake 环境代码 + 单元测试
4. 各方法训练好的模型权重（.pt）
5. 学习曲线、消融对比表、100-episode 终评、训练过程录像 montage
